const Productis = require("../../product");

// class que herda os atributos de product
module.exports = class Book extends Productis {
  constructor(title, synopsis, genre, pages, author, description, price, inStock)
};
